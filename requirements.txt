# Eye Care v3 — зависимости
# pip install -r requirements.txt

# Минимальный запуск (tkinter встроен в Python):
#   python main.py  — работает без трея

# Полный функционал:
pystray>=0.19.5     # системный трей
Pillow>=10.0.0      # иконка трея
pynput>=1.7.6       # умная пауза (мышь/клавиатура)
screeninfo>=0.8.1   # мульти-монитор
psutil>=5.9.0       # управление процессами
pygame>=2.5.0       # воспроизведение кастомных звуков (опционально)
